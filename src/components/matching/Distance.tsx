import { DistanceProps } from "@/type/ProfileCard/ProfileCard";

const Distance = ({distance, isDark} : DistanceProps) => {
    const fontStyle = `${isDark ? 'text-[#858585]' : 'text-[#2f2f2f]'} text-m mr-[0.2em] ${isDark ? '' : 'text-opacity-40'}`;

    return(
        <>
        </>
    )
}
import React from 'react';


const ProfileCardTag: React.FC = () => {

  const profileCardTagStyle = ``;
  const TagStyle = `font-bold text-[#2F2F2F] text-[0.7rem]`;

  return (
    <div className={profileCardTagStyle}>
      국내/국외 힙합
    </div>
  );
};

export default ProfileCardTag;
